<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for Hugging Face model download (HTTPS) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- POST_NOTIFICATIONS: user must grant this at runtime on Android 13+
         before any notification (including the download progress bar) is shown.
         The app requests it immediately on launch via MainActivity. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Required to start a foreground service (all API levels) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- Required on Android 14+ (API 34) when foregroundServiceType="dataSync" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <application
        android:name=".TurboQuantApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.TurboQuantAI"
        android:largeHeap="true"
        android:extractNativeLibs="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:hardwareAccelerated="true">

        <!--
            largeHeap: Gemma 2B Q4_K_M needs ~1.5 GB of heap.
            extractNativeLibs: libturboquant.so must be extracted to disk for dlopen().
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
            DownloadForegroundService:
            • foregroundServiceType="dataSync" satisfies Android 14+ foreground service
              restrictions for network I/O operations.
            • exported="false" — only started from within this app.
        -->
        <service
            android:name=".service.DownloadForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

    </application>

</manifest>
